# Agent Stack Overview

User → Local UI → FlameRouter → (Ollama / LM Studio) → Response Stream

This stack provides:
- Minimal latency
- High reliability
- No cloud dependency
- Predictable output behavior
