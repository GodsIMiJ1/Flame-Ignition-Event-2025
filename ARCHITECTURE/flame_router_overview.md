# FlameRouter — Local-First Routing Layer

FlameRouter is a custom-built orchestrator designed to:
- Route inputs to local models
- Manage fallback logic
- Monitor system load
- Provide stable streaming output

Its architecture allows cloud-like flexibility using only local compute.
